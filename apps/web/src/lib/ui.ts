// Shared panel surface styling. The full class string is written out
// literally so Tailwind's scanner picks it up at build time.
export const panelClass = "border border-[var(--line)] bg-[var(--panel)]";
